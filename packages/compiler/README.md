# @spotpatch/compiler

Framework-neutral JSX/TSX source filtering, marker injection, and source-map
generation shared by the SpotPatch framework adapters.

Applications should install a framework adapter such as `@spotpatch/vite` or
`@spotpatch/next`. This package is published to keep their dependency graphs
complete; it is not a standalone integration surface.
