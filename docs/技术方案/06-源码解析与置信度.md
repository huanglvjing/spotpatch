---
doc-id: "06-source-resolution"
title: "源码解析与置信度"
status: "active"
version: "1.1.0"
last-updated: "2026-09-05"
source-range: "规格书 §2.4 第 2 条、§11、§11.1–§11.5"
参考文献/依赖:
  - "02-architecture-stack"
  - "03-public-api-models"
  - "04-vite-plugin"
  - "12-testing-acceptance"
  - "15-risks-adr"
---

# 源码解析与置信度

Astro 补充（2026-09-05）：自身四段 `:astro` marker 对应 origin=astro-host、confidence=exact；只命中祖先仍是 dom-ancestor/approximate。普通三段 marker 保持 jsx-host。动态 HTML、lit-html 与未标记的 island 内部不能声称 exact。原生 `.astro` 不需要 React，ReactContext 可为 supported=false。格式由 shared 唯一 parser/formatter 定义；Astro 注入规则见 [Astro 规范](./Astro适配/00-方案与验收.md)。

本文件在总体架构规定的 AST/Fiber 分工内定义来源选择和置信度语义 (见 doc-id:02-architecture-stack)。类型和值域由核心数据模型唯一声明 (见 doc-id:03-public-api-models)，source marker 的格式与注入由 Vite 插件规范唯一声明 (见 doc-id:04-vite-plugin)。

## 解析顺序

```text
1. 目标 DOM 自身 source marker
2. React Adapter 返回的业务 composite source
3. 最近带 source marker 的 DOM 祖先
4. 无源码结果
```

## 置信度定义

| confidence | 条件 | UI 文案 |
| --- | --- | --- |
| exact | 目标 DOM 自身带 AST 标记 | 精确元素源码 |
| probable | Fiber 找到业务组件调用位置 | 可能的所属组件 |
| approximate | 只找到 DOM 祖先标记 | 最近业务容器 |
| unknown | 只有 DOM，没有可靠源码 | 未找到源码 |

不能为了“看起来功能完整”把 approximate 显示为 exact。

## React Adapter 接口

```tsx
export interface ReactAdapter {
  readonly name: string;
  supports(element: Element): boolean;
  inspect(element: Element): ReactContext;
  dispose(): void;
}
```

React 私有实现只能出现在 `react-adapter` 包。Runtime 不允许读取 `__reactFiber$*`、`_debugSource`、`return` 等字段。

## 业务组件识别

从 Host Fiber 向父级遍历时：

1. 跳过 React HostComponent。
2. 解析 `displayName`、函数名、class 名。
3. 展开 memo 和 forwardRef 的显示名。
4. 过滤 `Fragment`、`StrictMode`、Context Provider 等噪声节点。
5. 最多保留公共预算 `ContextBudget.maxComponentDepth` 限定的层数 (见 doc-id:03-public-api-models)。
6. 优先选择源码位于项目 root 的第一个 composite component。
7. 若只能定位 `node_modules`，继续向父级寻找业务组件。

Fiber source 在 React 18 中通常表示组件被调用的位置，不保证是组件定义行。模型中将其标为 `react-fiber/probable`。

## 降级行为

- React Adapter 失败时降级为 AST/DOM 模式。

- 没有 React：仍可选元素、收集 DOM/CSS、使用 AST 标记。
- React Adapter 抛错：捕获一次，禁用 adapter，本次会话继续运行。
- 未知 React 版本：显示“不支持的 React 版本”，不读取猜测字段。

本模块受 ADR-003 约束，所有 Fiber 兼容性风险和相关决策由风险与 ADR 文档登记 (见 doc-id:15-risks-adr)。验收场景见测试与验收规范 (见 doc-id:12-testing-acceptance)。
