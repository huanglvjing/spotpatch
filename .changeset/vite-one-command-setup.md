---
"@spotpatch/vite": minor
---

Add a one-command `spotpatch-vite setup` workflow that installs or upgrades `@spotpatch/vite@latest`, safely updates supported Vite configuration forms, and verifies the result. The initializer now supports object-returning `defineConfig` callbacks while continuing to reject ambiguous dynamic configurations without writing files.
