---
"@spotpatch/runtime": patch
"@spotpatch/vite": patch
---

Route Cursor and VS Code source navigation through the matching project workspace with exact line and column coordinates, and keep the native AI provider/model controls visually consistent with the dark workbench.
