---
"@spotpatch/bridge": patch
"@spotpatch/shared": patch
---

Allow Codex schema generation and Windows ACL verification enough time to complete on slower hosts while preserving the existing compatibility and access-control checks.
