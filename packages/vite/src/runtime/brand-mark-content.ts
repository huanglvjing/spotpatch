// Build-time trusted asset. The Vite server bundle carries it into the
// development virtual module; it is intentionally absent from runtime-client.js.
export { BRAND_MARK_CONTENT } from "../../../runtime/src/ui/brand-mark-content.js";
