# @spotpatch/dev-server

The Node-only SpotPatch development service: validated options, sessions,
source registration, local HTTP handlers, editor integration, and optional
Agent orchestration shared by framework adapters.

Applications should install `@spotpatch/vite` or `@spotpatch/next`. Browser
code must never import this package.
