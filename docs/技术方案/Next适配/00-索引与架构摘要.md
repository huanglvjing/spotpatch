---
doc-id: "next-00-index"
title: "Next.js 适配索引与架构摘要"
status: "active"
version: "0.6.0"
last-updated: "2026-08-26"
source-range: "Next.js 15.3–16.3 官方能力调研；SpotPatch Next.js 源码定位、数据链路与公共预览验证边界"
implementation-status: "public-preview"
参考文献/依赖:
  - "01-product-boundary"
  - "03-public-api-models"
  - "09-local-protocol-security"
  - "15-risks-adr"
  - "next-01-research-compatibility"
  - "next-02-package-architecture"
  - "next-03-integration-lifecycle"
  - "next-04-transform-source"
  - "next-05-sidecar-protocol"
  - "next-06-rsc-runtime"
  - "next-07-security-production"
  - "next-08-testing-delivery"
---

# Next.js 适配索引与架构摘要

## 文档状态声明

本目录描述 SpotPatch Next.js 适配的活动架构与 0.x 公共预览实现，不是“已经正式支持 Next.js”的声明。`implementation-status: public-preview` 表示 `@spotpatch/next` 已有可安装实现，但尚未完成 Gate N4 的全部版本、Router、Node、OS 与浏览器交互矩阵。只有正式支持门禁全部通过后，产品支持矩阵才能从“不支持”改为“正式支持” (见 doc-id:01-product-boundary)、(见 doc-id:next-08-testing-delivery)。

当前 `@spotpatch/vite` 的既有支持声明不变；Next.js 用户可以从 npm 安装 `@spotpatch/next` 公共预览，但不得把 `@spotpatch/vite` 当作 Next 插件，也不得把包可安装性等同于正式兼容或生产支持。

## 编码前复核结论

Gate N0、锁定版本 Loader POC、公共 `compiler`/`dev-server` 抽取和一个 Next 16.3/React 19.2 App Router 公共预览宿主已经完成。当前链路覆盖 CLI/config 配置请求、Sidecar、source/data-flow 原子注册、Turbopack/webpack Loader、pre-hydration recorder、共享数据链路面板、bootstrap、Runtime UI、源码上下文和生产 noop；Vite 仍使用同一 compiler/runtime/protocol/panel 实现。

仍然阻断正式支持的事实包括：

- external rewrite 的 NDJSON 流、取消、复杂 `basePath`/`trailingSlash` 与自定义 matcher 组合尚未完成 required fixture。
- React 19 只接受 compiler registration-backed 组件身份；独立模块实例下的全局 renderer 发现已有单测，真实浏览器最终复验仍是发布 Gate。marker、DOM、UI 可用不等于完整 React 19 组件栈支持。
- Pages/hybrid Router、Next 15 维护线、Windows、Node 20/22/24 和复杂宿主配置尚未跑完正式矩阵。
- 私有宿主 Runtime POC 已通过两种构建器的按钮交互与源码上下文断言，但尚未覆盖完整编辑器/Agent 链路和 required 浏览器矩阵。
- packed `@spotpatch/*` 的单一 Next 16 App Router fixture 已通过 Turbopack 干净生产 build/start：页面 200、私有接口 404，业务 HTML/RSC/client/server/static/source map 无 marker、recorder、panel、私有路由或 secret。Pages/standalone/static export/多 OS required fixture 仍未完成。

因此 0.x 公共预览必须继续保留醒目警告并补齐门禁；在门禁完成前不得修改产品支持矩阵或发布稳定支持声明。具体证据与缺口 (见 doc-id:next-08-testing-delivery)。

## 仓库决策

不创建第二个 Git 仓库。当前实现已在现有 Monorepo 增加 `@spotpatch/next`，并把 Vite/Next 共用的源码转换和开发服务能力抽成 `@spotpatch/compiler` 与 `@spotpatch/dev-server`。原因和包依赖边界 (见 doc-id:next-02-package-architecture)，正式 ADR (见 doc-id:15-risks-adr)。

仓库边界与 npm 边界不是同一个概念：

- 一个 Git 仓库维护共同协议、Runtime、Agent、安全规则、测试和版本图。
- 每个框架保留独立用户入口包；Vite 用户安装 `@spotpatch/vite`，Next.js 用户安装 `@spotpatch/next`。
- 用户不需要手动安装内部公共包；发布图必须闭合。
- 框架适配器不得相互依赖，Next 适配器不得通过安装 Vite 来复用实现。

## 目标架构摘要

```text
Next source modules
  → SpotPatch loader（Turbopack / webpack）
  → shared compiler prepared instrumentation
  → internal atomic source + component registration（registry epoch）
  → server: source marker only
  → browser: source marker + data-flow helper
  → Next rendered HTML / RSC payload / hydrated DOM
  → @spotpatch/next/client（pre-hydration hook + dispatch recorder）
  → shared @spotpatch/runtime + data-flow panel
  → same-origin /__spotpatch/v1/**
  → Next beforeFiles rewrite
  → loopback-only SpotPatch Sidecar
  → source/editor/Agent/Git/provider core

spotpatch-next dev
  → local Next child + authenticated loopback configuration
  → withSpotPatch resolves development config
  → owner configures the single Sidecar
```

该图只说明责任流，不重复 marker、HTTP endpoint、Agent 工具或公共配置的定义；这些事实继续分别由现有主题文档管理 (见 doc-id:03-public-api-models)、(见 doc-id:09-local-protocol-security)。Next 专属承载方式由本目录子文档定义。

## 文档地图

| 文档 | 单一职责 |
| --- | --- |
| 本文档 | 计划状态、仓库决策、总览和阅读顺序 |
| (见 doc-id:next-01-research-compatibility) | 官方能力证据、支持矩阵、明确不支持范围和待验证事实 |
| (见 doc-id:next-02-package-architecture) | 包边界、依赖方向、公共内核抽取和框架宿主接口 |
| (见 doc-id:next-03-integration-lifecycle) | 用户接入 API、配置组合、CLI 生命周期和迁移纪律 |
| (见 doc-id:next-04-transform-source) | Turbopack/webpack Loader、source marker、注册和 source map |
| (见 doc-id:next-05-sidecar-protocol) | Sidecar 进程、Next rewrite、双通道认证、关闭和恢复 |
| (见 doc-id:next-06-rsc-runtime) | App/Pages Router、RSC/Client Component、预水合与 Runtime 降级 |
| (见 doc-id:next-07-security-production) | 信任边界、生产零残留、缓存、路径和失败策略 |
| (见 doc-id:next-08-testing-delivery) | POC 门禁、测试矩阵、性能预算、Milestone 和发布条件 |

## 阅读与变更顺序

1. 先确认支持版本和不支持边界 (见 doc-id:next-01-research-compatibility)。
2. 再确认包边界和复用方式 (见 doc-id:next-02-package-architecture)。
3. 接入 API、转换、Sidecar 和 Runtime 分别在其唯一主题文档中演进。
4. 任何影响现有协议、安全或公共配置的决定，必须先修改其既有唯一事实源，不得在 Next 文档中复制定义。
5. 公共预览不得绕过剩余阻断项；只有 required matrix 全部通过，才允许发布稳定支持声明和修改支持矩阵 (见 doc-id:next-08-testing-delivery)。

## 核心工程判断

- Next 16 默认 Turbopack，因此只支持 webpack 不构成正式 Next 支持。
- Next 默认存在 Server Component，浏览器 Fiber 不能完整表达服务端组件树；精确 host marker 是主链路，React Adapter 只能增强。
- Bippy 必须在 React hydration 前安装 hook；使用 Next 官方 `instrumentation-client`，不能等待页面挂载后再初始化。
- Next 没有 Vite `configureServer` 等价公开钩子；本地文件、编辑器和 Agent 能力由独立 loopback Sidecar 承载。
- Sidecar 绑定 loopback 不足以抵御 Next external rewrite；首版 Next child 也必须强制 loopback，明确不支持 LAN。
- 父 CLI 不重复求值 `next.config` 或复制 Next `.env*` 规则；开发配置由真实 Next求值后经独立 secret 认证的 loopback内部请求配置唯一 Sidecar。
- 持久构建 cache 不能复用上一 CLI Session 的随机 fileId；非敏感 registry epoch 是 Loader cache identity 的组成部分。
- 不占用面向部署平台的 `adapterPath`，不使用会削弱 Next 优化的 Custom Server。
- 生产无可执行残留是正式支持条件；0.x 公共预览也不接受仅靠 `NODE_ENV` 运行时判断的“代码存在但不执行”。构建配置允许保留可审计的 `module id → side-effect-free noop` 映射，但业务 HTML/RSC/chunk/source map 不得包含实现代码。

上述判断对应的证据、细节和替代方案均在子文档及 ADR 中追溯，本文不维护第二份实现规则。
